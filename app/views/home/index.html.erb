<% arg = {
    todos: @todos&.map(&:to_json),
    today_todos: @today_todos&.map(&:to_json),
    overdue_todos: @overdue_todos&.map(&:to_json),
    tomorrow_todos: @tomorrow_todos&.map(&:to_json),
    due_undefined_todos: @due_undefined_todos&.map(&:to_json),
  }
%>

<%= render_react_component("Todo", arg: arg)%>
<%= javascript_tag 'components/Todo' %>