<% arg = {
    todos: @todos.map do |todo|
      {
        id: todo.id,
        title: todo.title,
        formatted_due_day: todo.formatted_due_day
      }
    end
  }
%>

<%= render_react_component("Todo", arg: arg)%>
<%= javascript_tag 'components/Todo' %>